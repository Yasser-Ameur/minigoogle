package com.minigoogle.query.lexer;

/** Enum of token types recognized by the lexer: WORD, PHRASE, AND, OR, NOT, LEFT_PAREN, RIGHT_PAREN. */
public enum TokenType {
    WORD,
    PHRASE,
    AND,
    OR,
    NOT,
    LEFT_PAREN,
    RIGHT_PAREN
}
